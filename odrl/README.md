# ODRL artifact family

This directory defines the **experimental ODRL artifact family** for the canonical trust artifact layer.

The purpose is narrow and practical: provide a stable machine-readable contract for carrying an ODRL policy or an ODRL policy reference inside trust infrastructure artifacts.

This does **not** turn this repository into an ODRL evaluator. It only standardizes the artifact shape so downstream systems can exchange policy references consistently.
