const d=require('../data/portfolio-relationships.json'); const req=['source_repository','target_repository','relationship_type','authority_direction','artifact_mapping','compatibility_range','maturity','validation','known_limitations','supersession']; for(const [i,x] of d.relationships.entries()){for(const k of req)if(!(k in x)){console.error(`relationship ${i} missing ${k}`);process.exit(1)}if(x.source_repository===x.target_repository)process.exit(1)} console.log(`Portfolio relationships: PASS (${d.relationships.length} declarations)`);
