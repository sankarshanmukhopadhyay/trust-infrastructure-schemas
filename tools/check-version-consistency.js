const fs=require('fs');const v=fs.readFileSync('VERSION','utf8').trim();const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));let f=[];if(pkg.version!==v)f.push(`package.json=${pkg.version}`);for(const [p,needle] of [['README.md',`v${v}`],['governance/repository-authority.yaml',`current_version: "v${v}"`],['CHANGELOG.md',`v${v}`]])if(!fs.readFileSync(p,'utf8').includes(needle))f.push(`${p} missing v${v}`);if(f.length){console.error('Version drift: '+f.join(', '));process.exit(1)}console.log(`Version consistency: PASS (v${v})`);
