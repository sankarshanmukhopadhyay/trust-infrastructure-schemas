# Security policy

Report schema bypasses, validator defects, unsafe defaults, misleading assurance claims, identifier collisions, or dependency vulnerabilities through a private GitHub security advisory where possible. Include reproduction steps, affected artifact identifiers, impact, and proposed containment.
