{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/sankarshanmukhopadhyay/trust-infrastructure-schemas/v0.5.1/credentials/dtg/v1/invitation-credential.json",
  "$credentialContext": [
    "https://www.w3.org/ns/credentials/v2",
    "https://firstperson.network/credentials/dtg/v1",
    "https://w3id.org/security/suites/ed25519-2020/v1"
  ],
  "$credentialType": [
    "VerifiableCredential",
    "DTGCredential",
    "InvitationCredential"
  ],
  "type": "object",
  "properties": {
    "invited": {
      "type": "string",
      "description": "DID of the invitee.",
      "pattern": "^did:[a-z0-9]+:[A-Za-z0-9.\\-_:%]+$"
    },
    "invitedTo": {
      "type": "string",
      "description": "DID of the VTC/VTN the invitee is being invited to.",
      "pattern": "^did:[a-z0-9]+:[A-Za-z0-9.\\-_:%]+$"
    },
    "inviter": {
      "type": "string",
      "description": "DID of the inviter/authorizer (may differ from VC issuer).",
      "pattern": "^did:[a-z0-9]+:[A-Za-z0-9.\\-_:%]+$"
    },
    "inviteNonce": {
      "type": "string",
      "description": "Nonce to prevent replay and bind acceptance to a specific invitation."
    },
    "validFrom": {
      "type": "string",
      "description": "RFC3339 timestamp when this invitation becomes valid.",
      "format": "date-time"
    },
    "validUntil": {
      "type": "string",
      "description": "RFC3339 timestamp when this invitation expires.",
      "format": "date-time"
    },
    "terms": {
      "type": "string",
      "description": "Reference to terms/policy governing acceptance (DID or URI)."
    }
  },
  "title": "VIC (Invitation Credential)",
  "description": "Authorizes a prospective member to join a VTC or VTN when presented to the VTA/PEP.",
  "required": [
    "invited",
    "invitedTo",
    "validUntil",
    "inviteNonce"
  ]
}
