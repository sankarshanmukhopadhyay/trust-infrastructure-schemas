{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/sankarshanmukhopadhyay/trust-infrastructure-schemas/v0.5.1/credentials/dtg/v1/membership-credential.json",
  "$credentialContext": [
    "https://www.w3.org/ns/credentials/v2",
    "https://firstperson.network/credentials/dtg/v1",
    "https://w3id.org/security/suites/ed25519-2020/v1"
  ],
  "$credentialType": [
    "VerifiableCredential",
    "DTGCredential",
    "MembershipCredential"
  ],
  "title": "VMC (Membership Credential)",
  "type": "object",
  "properties": {
    "member": {
      "type": "string",
      "description": "DID of the member node being enrolled.",
      "pattern": "^did:[a-z0-9]+:[A-Za-z0-9.\\-_:%]+$"
    },
    "memberOf": {
      "type": "string",
      "description": "DID of the VTC/VTN the member belongs to.",
      "pattern": "^did:[a-z0-9]+:[A-Za-z0-9.\\-_:%]+$"
    },
    "membershipRole": {
      "type": "string",
      "description": "Role of the member within the trust community/network.",
      "enum": [
        "member",
        "operator",
        "admin",
        "observer"
      ]
    },
    "status": {
      "type": "string",
      "description": "Current membership status.",
      "enum": [
        "active",
        "suspended",
        "revoked"
      ]
    },
    "effectiveFrom": {
      "type": "string",
      "description": "RFC3339 timestamp when this membership becomes effective.",
      "format": "date-time"
    },
    "effectiveUntil": {
      "type": "string",
      "description": "RFC3339 timestamp when this membership expires (optional).",
      "format": "date-time"
    },
    "governanceReference": {
      "type": "string",
      "description": "Reference to governance/policy governing this membership (DID or URI)."
    }
  },
  "description": "Establishes a node in the DTG by defining membership in a VTC or VTN.",
  "required": [
    "member",
    "memberOf",
    "status",
    "effectiveFrom"
  ]
}
