{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/sankarshanmukhopadhyay/trust-infrastructure-schemas/v0.7.0/credentials/agent/v1/identity-link.json",
  "$credentialContext": [
    "https://www.w3.org/ns/credentials/v2",
    "https://schemas.archetech.com/credentials/agent/v1"
  ],
  "$credentialType": [
    "VerifiableCredential",
    "AgentCredential",
    "IdentityLinkCredential"
  ],
  "title": "Agent Identity Link",
  "description": "Credential linking an Archon DID to identity on another platform",
  "type": "object",
  "properties": {
    "platform": {
      "type": "string",
      "description": "The platform being linked (e.g., 'nostr', 'lightning', 'github', 'twitter')"
    },
    "platformId": {
      "type": "string",
      "description": "The identifier on that platform (pubkey, username, address)"
    },
    "platformIdType": {
      "type": "string",
      "description": "Type of identifier (e.g., 'npub', 'hex-pubkey', 'lightning-address')"
    },
    "proofType": {
      "type": "string",
      "enum": [
        "self-attested",
        "signature",
        "dns",
        "on-chain",
        "social-post"
      ],
      "description": "How the link was proven"
    },
    "proofReference": {
      "type": "string",
      "description": "Reference to the proof (note ID, transaction, URL)"
    },
    "verifiedAt": {
      "type": "string",
      "format": "date-time",
      "description": "When the link was verified"
    }
  },
  "required": [
    "platform",
    "platformId",
    "proofType"
  ]
}
